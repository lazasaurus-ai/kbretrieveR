% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kb_ask.R
\name{kb_ask}
\alias{kb_ask}
\title{Retrieve and generate from a Bedrock Knowledge Base}
\usage{
kb_ask(
  question,
  kb_id,
  model_id = NULL,
  profile = NULL,
  region = NULL,
  retrieval_cfg = list(),
  generation_cfg = list(),
  guardrails_cfg = NULL,
  return = c("tidy", "raw")
)
}
\arguments{
\item{question}{A character string with the user query.}

\item{kb_id}{The Knowledge Base ID to query.}

\item{model_id}{Optional model ARN to use for generation.}

\item{profile}{Optional AWS CLI profile to use.}

\item{region}{Optional AWS region to use.}

\item{retrieval_cfg}{Optional list of retrieval configuration parameters (e.g., \code{list(topK = 6)}).}

\item{generation_cfg}{Optional list (e.g., \code{list(maxTokens = 4000, temperature = 0)}).}

\item{guardrails_cfg}{Optional guardrail configuration list.}

\item{return}{One of \code{"tidy"} or \code{"raw"}. Default \code{"tidy"}.}
}
\value{
A tibble (tidy) or the raw paws response.
}
\description{
Calls Bedrock Agent Runtime \code{retrieve_and_generate()} to both retrieve context
and generate an answer. Returns a tidy tibble (answer, citations, usage, raw).
}
